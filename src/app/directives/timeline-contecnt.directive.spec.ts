import { TimelineContecntDirective } from './timeline-contecnt.directive';

describe('TimelineContecntDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineContecntDirective();
    expect(directive).toBeTruthy();
  });
});
