import { SidemenuColorDirective } from './sidemenu-color.directive';

describe('SidemenuColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SidemenuColorDirective();
    expect(directive).toBeTruthy();
  });
});
